import toDate from './toDate.filter';
import toHour from './toHour.filter';
import movementSessionDuration from './movementSessionDuration.filter';
import movementSessionTime from './movementSessionTime.filter';

export default {
  toDate,
  toHour,
  movementSessionDuration,
  movementSessionTime,
};
